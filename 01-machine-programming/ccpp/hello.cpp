#include <cstdio>                   // Includes the cstdio library

int main() {
    std::printf("Hello Sekai!\n");  // Prints the string "Hello Sekai!\n" to console 
    std::getchar();                 // Waits for ENTER to be pressed(halts the program).
    return 0;                       // Returning the value zero because program runs successfully.
}